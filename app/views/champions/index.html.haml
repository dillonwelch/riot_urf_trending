.content
  %main.index
    %table
      - index = 0
      - @champions.each_slice(5) do |slice|
        %tr
          - slice.each do |champion|
            - index += 1
            %td 
              = image_tag riot_image_link champion.name
              = "#{index}: "
              = champion.name
              %dl.inline
                %dt= "Win rate: "
                %dd= (champion.win_rate * 100).round(2)
                %dt= "Pick rate: "
                %dd= (champion.pick_rate * 100).round(2)
