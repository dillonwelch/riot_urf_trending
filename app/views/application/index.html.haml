.banner
  %h1
    Top 5 champions in the last hour for URF
.content
  %main
    .charts
      - (1..5).each do |num|
        %h2
          %span{ id: "title#{num}" }
          %img{ id: "image#{num}", height: '40px' }
        %canvas{ id: "chart#{num}" }
  %aside
    bearsbearsbears
