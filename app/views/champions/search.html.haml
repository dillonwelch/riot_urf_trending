.content
  %main.show
    %aside
      = image_tag riot_image_link @champion.name
      %h1= @champion.name
      %ul
        %li= "Win rate: #{(@champion.win_rate * 100).round(2)}"
        %li= "Pick rate: #{(@champion.pick_rate * 100).round(2)}"
